/// Balanced JSON backup — only [formatVersion] `1` is supported (export and import).
class BackupConstants {
  static const String formatId = 'balanced_backup';
  static const int formatVersion = 1;
}
